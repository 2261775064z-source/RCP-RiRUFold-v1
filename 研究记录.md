# RCP-RiRUFold 方法研究与代码测试记录

> 2026-09-14 更新：RCP 已收敛为两个独立研究点，并将背景模块改为无尺度偏置的中位偏差融合、将 `U_B/uncertainty` 统一改为 `Delta_B/scale_disagreement`。最新公式—代码—实验合同见 `RCP两个研究点_公式代码实验对照.md`；下文保留为研究过程记录。

## 1. 研究结论先行

本轮形成一条可进入服务器训练的主线：**RCP-RiRUFold
(Reliability-Calibrated Patch-Consensus RiRUFold)**。它不是把旧 ADMM 模块再堆一层
卷积，而是修正了五个会影响论文论证或数值复现的问题：整图二维 SVD 与“张量模式”
表述不符、物理稀疏强度和 logits 混用、学习校正没有硬幅值界、batch 共享罚参数、
动态 `mu` 后未重标 scaled dual。完整公式与禁止主张见 `题目分析报告.md`。

支线 **RCP-Blob** 已实现并扫描，但本地门禁未通过。低 `gamma_g` 满足空场约束却
没有稳健目标救援；`gamma_g>=0.20` 出现中位目标响应时，空场超阈比例又明显超过
预注册上限。因此批量服务器脚本默认排除它，只保留代码供探索或负结果复核。

## 2. 根源材料与快照

| 文件 | SHA-256 | 本轮作用 |
|---|---|---|
| `docs/manuscript_zb.pdf` | `F98AB0ECCFE7BCE1530BD5E94538140C56D720F359D21949195D1A39816CEA69` | RiRUFold/SRMT 小目标主方向、低秩与重加权稀疏展开 |
| `docs/TKDE-2026-08-2894_Proof_hi (1).pdf` | `C763FFD1D322D2CCC5C4665373E76864ACE590ACA342777BA971CE3E8A2CBE71` | SCLT 中多信息源、显式目标、交替更新、残差与边界论证方式 |
| `docs/RiRUFold_ADMM_论据化改进方案.docx` | `45F91E2F96150FBEE20AB87D89B32AC65E0D7EDECCAC4F06E5F7CE89A5CA5B35` | 原改进方案与待核查主张 |
| `docs/RiRUFold_ADMM_Design_Plan.md` | `9B7D27B8CA5CABB08F63BB86F3C547103E19AE26A9C4DA50F672882A34678B04` | 原设计路线与实验拆解 |
| `docs/RiRUFold_ADMM_Evidence_Based_Design.md` | `35FA376EAE2B8E740B6CF86100E56911F77005C87787B33AC459E83B749108BE` | 证据链与可证伪条件基础 |

PDF 原件未修改；检索文本仅保存于 `research_work/source_text/`。补充核验使用的公开
一手资料包括 [RPCANet (WACV 2024)](https://openaccess.thecvf.com/content/WACV2024/html/Wu_RPCANet_Deep_Unfolding_RPCA_Based_Infrared_Small_Target_Detection_WACV_2024_paper.html)、
[Reweighted Infrared Patch-Tensor Model](https://doi.org/10.1109/JSTARS.2017.2700023)、
[reweighted l1 minimization](https://doi.org/10.1007/s00041-008-9045-x) 与
[Boyd 等人的 ADMM 综述](https://stanford.edu/~boyd/papers/admm_distr_stats.html)。这些
资料只支撑方法来源与约束，不替代两个本地根源论文。

## 3. 对原改进方案的主要质疑

1. 原实现对 `B×1×H×W` 直接做二维 SVD，只能称整图矩阵低秩，不能支撑多模
   tensor unfolding 的论文主张。
2. 原训练把同一个 `out_target` 同时送入 Soft-IoU 和 `X-B-S`；logits 是无量纲
   判别量，`S` 必须留在输入强度域，两种语义不能共用。
3. 原卷积校正只限制 gate 系数，卷积输出本身无界；新实现改为
   `gamma*local_scale*tanh(net)`，逐像素硬界可直接断言。
4. 原 `mu=5*image.std()` 是 batch 标量，导致某张高动态范围图改变同 batch 其他图
   的阈值；新实现固定为 `B×1×1×1`。
5. 原动态 `mu` 更新后继续沿用 scaled dual。新实现先形成 `U*`，再乘
   `mu_old/mu_new`，保持同一未缩放乘子语义。
6. 原 `WLS*WSE` 固定乘积默认两路证据同等可靠。新主线通过 patch 分支不一致性
   控制结构通道指数，并在 log 域稳定归一化。

## 4. 新主线的代码结构

- 三尺度 `(patch,stride)=(8,4),(12,6),(16,8)`，每个样本形成
  `p^2×L` patch 矩阵做 SVT，再 fold/coverage/crop。
- 三个背景锚点到逐像素中位中心的偏差形成 softmax 共识权重；加权方差只解释为
  “背景分支不一致性代理”，不称预测不确定性。
- 固定 Sobel + 固定 `5×5` 二项核定义结构张量；结构证据与倒数稀疏证据先做
  逐样本空间标准化，再按可靠性在 log 域几何池化。指数实现先减空间最大值。
- 背景与稀疏两侧均保留显式锚点，并加 `tanh` 硬界校正；`L_prox` 先逐样本
  归一化，再做 batch/stage 均值，避免正则项重新引入 batch 耦合。
- 每阶段计算逐样本 primal/dual 残差、受限 `alpha`、动态 `mu` 和 dual 重标。
- `aux["sparse_intensity"]` 是分解损失唯一允许使用的目标状态；logits 使用固定输入
  对比度尺度 `rho_X>=0.01` 单调读出。
- 对平坦 patch 的重复奇异值，推理/前向仍使用精确 SVT；训练反向使用确定性的
  小对角扰动代理并以直通方式保持相同前向值。它是防 NaN 的梯度策略，不是精确
  SVT 导数或收敛证明。

主实现位于 `models/rirufold_rcp.py`，已注册主线、固定乘积、无反馈、整图 SVD、
Blob 和 signed 六个名字。`integration_reference/` 给出原工程的注册和训练入口修改；
`scripts/evaluate_rirufold_rcp.py` 与 `scripts/diagnose_rirufold_rcp.py` 分别用于独立
测试和阶段状态导出。

论文表述、网络 Mermaid 流程、创新到类/函数的逐项映射，以及最新服务器集成、
单模型训练、三数据集三种子批量训练、自动测试和结果汇总命令，统一写入
`创新方法与服务器复现指南.md`。集成训练入口新增确定性的 `--run-name`；批量脚本
使用固定末轮 `latest.pkl`，并将 seed 写入每个评测 JSON，避免时间戳目录和手工
抄数破坏“数据集—模型—种子—权重—指标”的追溯关系。

## 5. 本地代码门禁

命令：

```powershell
cd D:\python_DM\RPCANet-main\RiRUFold_ADMM
.\.venv\Scripts\python.exe scripts\smoke_test_rirufold_rcp.py
```

环境为 Python 3.12.14、CPU PyTorch 2.14.0。最终烟测包含：

| 检查 | 结果 |
|---|---:|
| 非整除尺寸、零阈值 patch fold 最大误差 | `1.1920929e-6`（门槛 `<1e-5`） |
| 获得有限梯度的参数张量数 | `25` |
| 结构化输入梯度最大绝对值 | `5.3831e-5`，有限 |
| 平坦输入梯度最大绝对值 | `4.7558e-3`，有限 |
| batch 组成改变造成的背景/logits 最大误差 | `0 / 0` |
| `L_prox(pair)` 与单样本均值差 | `0` |
| logits 导数范围 | `[38.9143,40.7223]`，正且低于上界 `400.0001` |
| 真实链路 | `datasets/NUDT-SIRST/test/images/000001.png` resize 到 `40×40`，trace 通过 |

同时断言了三尺度数量、`mu` 的 `B×1×1×1` 形状、`W/alpha/mu` 范围、双侧
校正硬界、强度/logits 不共享存储，以及四个主要消融的两输出合同。

## 6. 数据剖析与未训练机制结果

本地 NUDT-SIRST test 目录含 664 对图像/掩膜，逐文件哈希、尺寸、强度与掩膜统计
见 `results/nudt_test_profile.csv`。8 邻域统计共得到 945 个目标连通域；每图目标
像素中位数为 39，范围 5–178；图像强度标准差中位数为 0.1232，范围
0.00978–0.37145。未删图、未删连通域、未用这些 test 图训练或选择权重。

固定 seed `20260910` 的 8 个合成强边缘/亮点场景中，未训练主线的边缘稀疏泄漏
均值为 `0`，固定乘积为 `1.78822e-4`；主线通过“不得高于乘积 1.05 倍”的机制
门槛。这个结果只说明初始化的抑边方向，没有证明训练后的 mIoU/Pd/Fa。

未训练主线的 target 稀疏响应中位数同样为 `0`，因此不能把本地结果写成已具备
检测能力。Blob 扫描结果如下：

| `gamma_g` | target 中位响应 | target/edge 中位比 | 场景达标率 | 空场超阈增量（百分点） | 本地门禁 |
|---:|---:|---:|---:|---:|---:|
| 0.05 | 0 | 0 | 0% | 0.0078 | FAIL |
| 0.10 | 0 | 0 | 12.5% | 0.0781 | FAIL |
| 0.20 | 0.002041 | 20.41 | 50.0% | 0.4531 | FAIL |
| 0.30 | 0.007073 | 70.73 | 87.5% | 0.8047 | FAIL |
| 0.40 | 0.008564 | 85.64 | 100% | 1.0703 | FAIL |

失败原因很清楚：低强度支线满足 `<=0.1` 个百分点空场约束但没有稳健救援；高强度
支线提高响应却产生过多空场激活。因此当前结论是“实现可用、动机可研究、尚不应
进入默认服务器矩阵”。对应原始逐场景表见 `results/blob_gamma_scene_metrics.csv`，
权衡图见 `figures/result_q1_blob_tradeoff.svg`。

## 7. 服务器正式比较建议

默认矩阵比较：旧 `rirufold_admm`、`rirufold_rcp_product`、`rirufold_rcp`、
`rirufold_rcp_nofeedback`、`rirufold_rcp_global`。固定 NUDT-SIRST、IRSTD-1K、
SIRST-Aug 的 split、epoch、batch、优化器、学习率和 stage 数，每个配置至少三个
seed。主结果报告 mIoU、F1、Pd、Fa、参数量、显存和单图推理时间；诊断量报告最终
primal/dual residual、patch 不一致性、目标区/强边缘区阈值及校正/锚点比。

主线相对 `rcp_product` 的预注册支持门槛为：至少 2/3 数据集的三种子平均 mIoU
提高不低于 0.30 个百分点，Fa 相对增加不超过 5%，且高结构/低 SCR 子集的 Fa
相对降低至少 5% 或 Pd 提高至少 0.50 个百分点。未达到时应报告不确定或负结果，
不能用更低分解残差替代检测指标。

批量命令：

```bash
bash RiRUFold_ADMM/scripts/run_server_ablation.sh
```

脚本默认不含 Blob；只有显式设置 `INCLUDE_BLOB=1` 才会加入该支线。最终评测必须
使用 `scripts/evaluate_rirufold_rcp.py` 重新读取 test split，并保存 JSON 结果。

## 8. 当前边界

- 本机未进行任何模型训练，也没有生成 mIoU/F1/Pd/Fa 对比；这些结论必须由用户
  在服务器完成，不能从本地机制图推断。
- 三尺度 SVD 比纯卷积路径更耗时、显存更高；服务器应实测而不是只报参数量。
- 重复奇异值的稳定反向是代理梯度，应跟踪 NaN/Inf 和梯度范数，并可增加“不使用
  代理”的受控消融来量化其训练影响。
- 当前默认非负稀疏仅适用于亮目标。存在暗目标的数据集应使用 signed 支线并单独
  验证，不能把非负主线直接外推。
- 本地仓库当前可用；每轮仍同时保存逐文件 SHA-256、复现清单、确定性命令和独立
  门禁回执，避免只依赖提交号追踪实验输入。

## 9. 第二套方案：CMC-RiRUFold（2026-09-12）

第二套方案侧重**背景观测与补全**，不重复第一方案侧重的可靠性阈值融合。最终主线
为 Counterfactual Masked-Completion RiRUFold：阶段候选观测排除、固定行随机环形侧
先验、两次 patch 低秩交换、严格正精度共识、候选条件稀疏近端，以及逐样本
`mu/scaled-dual` 更新。完整推导和服务器命令见 `CMC创新方法与服务器复现指南.md`。

### 9.1 文献边界与方案质疑

补充双引擎检索找到 Yan 等人的 *Robust Infrared Superpixel Image Separation Model for
Small Target Detection*（JSTARS 2021，DOI `10.1109/JSTARS.2021.3116965`），其中已有
outlier superpixel masking。因此 CMC 不主张“首次掩膜”，创新范围收缩为候选观测
排除、同图受约束侧信息、多步补全和正精度共识的联合展开。

第一次 40×40 合成门禁使用了约 0.6% 的目标面积，而本地 NUDT-SIRST 664 张测试掩膜
的每图目标像素中位数为 39，即 `39/65536≈0.06%`。前者放大约 10 倍，会错误地让末
阶段面积日程看似过小。本轮没有据此修改模型，而是把合成目标支持校准到真实数据量级
后重新运行。该校准只决定机制测试场景，不使用 test mask 训练或调学习参数。

### 9.2 M1/P1 门禁

- M1 最终 PASS：`noside` 完全切断 `mathcal A_k`，`p_A=0`、`L_cf=0`；符号、四个
  消融和失败条件闭合。
- P1 PASS：模型、注册、训练损失与烟测一致；五文件 `py_compile` 和
  `scripts/smoke_test_rirufold_cmc.py` 均退出码 0。
- 后续补充 P1 的 P2 覆盖：面积日程端点为 `0.02/0.000625`（40×40、5 stages），
  `HW<50` 被拒绝，完整 `J=2` 阶段侧先验恰好调用 3 次。

关键数值合同：常量保持最大误差 `1.1921e-7`，平坦候选最大值 `0`，两次 inner
更新最大变化 `0.11542/0.04616`，最小融合分母约 `3.0`，目标/背景候选均值
`0.92129/0.000149`，batch 背景/logits 最大差 `0/0`，随机与平坦输入反向梯度有限。

### 9.3 未训练机制结果

固定 seed `20260912`、8 个按 NUDT 目标占比校准的场景、相同权重初始化下：

| 配置 | 目标区背景吸收 MAE↓ | 目标-边缘稀疏对比↑ | 边缘稀疏响应↓ |
|---|---:|---:|---:|
| CMC | 0.03620 | 0.38280 | 0.000372 |
| No mask | 0.41787 | 0 | 0 |
| No side | 0.41556 | 0 | 0 |
| One pass | 0.03639 | 0.38357 | 0.000334 |
| Fixed mask | 0.04340 | 0.37301 | 0.001620 |

主线相对 `nomask/noside` 明显减少目标吸收，且未把强边缘大量转移到稀疏图，支持
核心“改变观测+有约束侧先验”的机制方向。`onepass` 与两步主线非常接近：主线的目标
吸收略低，但 onepass 的边缘响应和分解残差略低。因此当前不能宣称“两步一定优于
一步”，该子结论保留到三数据集训练后裁决。`fixedmask` 边缘泄漏更高，支持阶段重估
的动机，但同样不能代替训练结果。

### 9.4 输出与服务器矩阵

原始表为 `results/cmc_*.csv/json`；九组 q2 图均保存 SVG、300 DPI PNG 和灰度 QA，
自动 `figure_audit --questions q1 q2 --strict` 通过，并逐张人工检查。服务器默认同时
比较旧 ADMM、第一方案 RCP、CMC 主线与四个 CMC 消融，固定三个种子、三个数据集和
相同预算。批量入口：

```bash
bash RiRUFold_ADMM/scripts/run_cmc_server_ablation.sh
```

本机只做机制、数值、单图评测链路测试，没有训练并且没有产生可写入论文主表的
mIoU/F1/Pd/Fa。

### 9.5 审稿人视角收口（继续思考）

复核发现原 `fixedmask` 实现虽然缓存 `C^0/V^0`，却按后续阶段的收缩日程重新计算
budget loss，导致一个不能改变的 2% 掩膜被更小预算额外惩罚。这不是严格单变量消融。
现已同时冻结第 0 阶段的候选图和面积预算；新增 smoke 断言保证后续阶段二者均与
第 0 阶段完全一致。输出状态没有改变，机制指标保持不变，训练损失比较则更公平。

服务器证据链新增 `scripts/analyze_cmc_server_results.py`。它要求 63 个预注册实验单元
全部齐全，拒绝重复单元、partial test、同数据集 split hash 或 threshold 不一致；随后
还校验实际/完整测试图数量一致及 checkpoint SHA-256 未跨单元复用；随后自动写出逐
种子数据、mean±std、CMC 相对各对照的配对增益和描述性 bootstrap CI，
以及主比较、消融、CMC-vs-RCP 配对增益三组 PNG/SVG 论文图和 Markdown 报告。
`scripts/smoke_test_cmc_result_analysis.py` 用完整的 63 单元合成元数据验证了该链路；
合成数值仅存在临时目录并在测试后删除，不进入论文结果或仓库 figures。

本地 `cmc_model_complexity.csv` 不再记录波动的 CPU wall time，只保留参数量、阶段数、
inner 次数及 mask/side/fixedmask 开关等确定性结构字段。正式速度统一读取服务器完整
评测 JSON 的 `seconds_per_image`，避免每次复现仅因本机调度不同而改变证据哈希。
