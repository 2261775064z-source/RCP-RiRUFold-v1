# RCP-RiRUFold 术语与符号表

| 中文术语 | English | 符号/代码名 | 定义 | 单位/范围 | 首次出现 | 禁止混用 |
|---|---|---|---|---|---|---|
| 观测红外图像 | observed infrared image | `X`, `image` | 归一化单帧灰度输入 | 无量纲归一化强度，默认 `[0,1]` | 分析报告 4.1 | 不称三阶时空张量，除非输入真实含时间/谱维 |
| 背景分量 | background component | `B^k`, `background` | 第 `k` 阶段的低秩背景估计 | 与 `X` 同单位 | 4.1 | 不与背景特征 `feature` 混用 |
| 稀疏强度分量 | sparse intensity component | `S^k`, `sparse_intensity` | 参与 `X=B+S+R` 的目标强度估计 | 与 `X` 同单位；主线非负 | 4.1 | 严禁称 logits 或 probability |
| 分割对数几率 | segmentation logits | `ell`, `logits` | 由 `S^K` 经同一输入图像的固定正对比度尺度 `rho_X>=0.01` 单调标定得到的无量纲判别量 | 实数、无量纲 | 4.7 | 严禁用于 `L_dc` |
| 分割概率 | segmentation probability | `sigmoid(logits)` | 评测与阈值化所用概率 | `[0,1]` | 4.7 | 不与稀疏强度混用 |
| 缩放对偶变量 | scaled dual variable | `U^k`, `dual` | 采用 `X-B-S` 符号约定的 ADMM-inspired 对偶状态 | 与 `X` 同单位 | 4.1 | 不称未缩放 Lagrange multiplier |
| 罚参数 | penalty parameter | `mu_k`, `mu` | 控制近端阈值和残差平衡；每样本一个标量 | 正数，默认裁剪 `[0.05,20]` | 4.1 | 不允许 batch 共享后仍称逐样本自适应 |
| 块提升算子 | patch lifting operator | `P_s`, `unfold` | `B×1×H×W -> B×p_s²×L_s` 的重叠块矩阵；默认三尺度 `(8,4)/(12,6)/(16,8)` | 线性算子 | 4.2 | 不直接称 tensor mode unfolding |
| 块回投算子 | patch aggregation/folding operator | `P_s^dagger`, `fold` | 相同 padding/stride 的重叠块回投并除以覆盖计数 | 线性算子 | 4.2 | 不省略覆盖归一化或边界裁剪 |
| 奇异值阈值 | singular value thresholding | `SVT_tau` | `U diag((sigma-tau)_+) V^T` | 与输入强度相关 | 4.2 | 不称指数复杂度；不等同自由 CNN |
| 多尺度块提升低秩估计 | multi-scale patch-lifted low-rank estimation | `B_s`, `scale_backgrounds` | 对三个 `p_s^2×N_s` 块提升矩阵分别做 SVT 并覆盖归一化回投 | 与 `X` 同数值域 | 当前研究点一 | 不称多个 tensor modes、独立区域分解或模糊观点 |
| 尺度背景估计 | scale-conditioned background estimate | `B_s`, `scale_backgrounds[:,s]` | 给定 `(p_s,h_s)` 的块提升、SVT、覆盖归一化回投结果 | 与 `X` 同数值域 | 当前研究点一 | 不声称图像域重叠核范数的精确 proximal |
| SVT 稳定反向代理 | stabilized SVT backward surrogate | `D`, `_svt_matrix` | 前向保持精确 SVT，仅反向用尺度感知的固定小对角扰动拆分重复奇异值 | 梯度策略 | 4.2 | 不称精确 SVT 导数；推理不启用 |
| 中位偏差融合权重 | median-deviation fusion weight | `omega_s`, `scale_weights` | `softmax(-kappa*d_s)`；无尺度偏置，且对本尺度偏差严格单调递减 | `[0,1]`，尺度和为 1 | 当前研究点一 | 不称统计后验概率 |
| 归一化尺度分歧 | normalized inter-scale dispersion | `Delta_B`, `scale_disagreement` | 三个尺度背景估计相对融合结果的归一化加权标准差 | 非负、无量纲 | 当前研究点一/二 | 不称置信区间；共同误差无法被它识别 |
| 结构张量相干性 | structure-tensor coherence | `c(Y)`, `c_B`, `coherence` | 固定归一化 Sobel 梯度、固定 5×5 二项平滑后，`(lambda_1-lambda_2)/(lambda_1+lambda_2+epsilon_tensor)` 并裁剪 | `[0,1]` | 4.4 | `c_B=c(B)`；支线 `c(Z_S)` 复用同一算子；不单独解释为目标显著性 |
| 结构惩罚证据 | structural penalty evidence | `q_B`, `log_w_ls` | 背景相干性与梯度能量形成的 log-penalty 证据 | 无量纲 | 4.4 | 高值表示应加强稀疏惩罚，不表示目标概率高 |
| 稀疏惩罚证据 | sparse penalty evidence | `q_S`, `log_w_se` | 当前 `S` 的倒数重加权 log-penalty | 无量纲 | 4.4 | 倒数重加权本身不称新原理 |
| 结构证据门控 | structural-evidence gate | `pi_B`, `structure_gate` | `sigmoid(a0-ad*Delta_B)`；`ad>0`，随尺度分歧严格单调递减 | `(0,1)` | 当前研究点二 | 未经校准实验不称可靠性概率 |
| 分歧门控双证据池化 | disagreement-gated dual-evidence log pooling | `h_W` | `pi_B*q_B+(1-pi_B)*q_S` | 无量纲 | 当前研究点二 | 不写成固定 `WLS*WSE` |
| 最终稀疏权重 | final sparse penalty weight | `W_RCP`, `w_rcp` | 显式控制加权软阈值的正权重 | 默认 `[0.25,4]`，均值约 1 | 4.4 | 不称二值 mask；不称 attention map |
| 非负软阈值 | nonnegative soft threshold | `positive_soft_threshold` | `max(z-theta,0)`，亮目标稀疏近端 | 与 `z` 同单位 | 4.5 | 不用于含暗目标数据而不修改假设 |
| 学习校正 | learned correction | `delta_B`, `delta_S` | 显式背景/稀疏更新之后的 `tanh` 有界修正 | 与相应状态同单位 | 4.3/4.5 | 不直接称 proximal operator |
| 校正硬界 | correction hard bound | `gamma_max * scale` | 校正逐像素绝对值上界 | 与相应状态同单位 | 4.3 | 不用“小初始化 gate”代替硬界 |
| 原始残差 | primal residual | `R^k`, `primal_residual` | `X-B^k-S^k` | 与 `X` 同单位 | 4.6 | 不与训练总 loss 混用 |
| 对偶残差代理 | dual residual proxy | `d_k`, `dual_residual` | `mu_k ||S^(k+1)-S^k||_F` | 强度范数 | 4.6 | 对本非经典展开不声称严格 KKT 证书 |
| 对偶预条件步长 | dual preconditioner | `alpha_k`, `alpha` | 残差写入 scaled dual 的受限逐像素步长 | `(0,1]` | 4.6 | 不称 exact ADMM step |
| 对偶重标 | scaled-dual rescaling | `mu_old/mu_new` | 罚参数变化时保持未缩放乘子一致 | 正比率 | 4.6 | 不可在动态 `mu` 下省略而称同一 scaled dual |
| 分解一致性损失 | decomposition-consistency loss | `L_dc` | `mean(abs(X-B-S))` | 强度 | 6 | 严禁把 `logits` 代入 |
| 背景损失 | target-excluded background loss | `L_bg` | `mean(abs((1-M)*(B-X)))` | 强度 | 6 | 不称完整背景真值监督 |
| 近端偏离损失 | proximal-deviation loss | `L_prox` | 每个样本先计算校正相对其局部强度尺度的平均幅值，再对 batch/阶段求均值 | 无量纲 | 6 | 禁止先跨 batch 平均再作均值之比；不强迫模型完全复制手工近端 |
| 空间标准化 | per-sample spatial standardization | `N(q)` | 逐样本减空间均值并除以带 `epsilon_N` 的空间 RMS | 无量纲 | 4.4 | `S^0=0` 时输出零，不制造权重纹理 |
| 各向同性亮点救援 | isotropic blob rescue | `blob_rescue` | 以固定四邻域 Laplacian 的正亮点响应和同一结构张量算子的低方向相干性降低局部阈值 | 无量纲 | 5 | 未经真实训练不得列为主贡献 |
| 信杂比 | signal-to-clutter ratio | `SCR` | 目标与局部背景的对比/杂波尺度比；具体口径须固定 | 通常无量纲 | 8.2 | 不与 SNR 随意互换 |
| 检测概率 | probability of detection | `Pd` | 目标级命中率，连接域匹配口径需固定 | `[0,1]` 或 `%` | 8.2 | 不与像素 recall 混用 |
| 虚警率 | false-alarm rate | `Fa` | 误报像素数/总像素数，论文常报 `10^-6/pixel` | 每像素 | 8.2 | 不与 `1-precision` 混用 |
| 交并比 | intersection over union | `mIoU` | `TP/(TP+FP+FN)` 的数据集聚合值 | `[0,1]` 或 `%` | 8.2 | 必须说明宏/微聚合口径 |
| 反事实掩蔽补全展开 | counterfactual masked-completion unfolding | `CMC-RiRUFold` | 阶段式排除目标候选的背景观测，以行随机侧先验和多步低秩补全恢复无目标背景的展开模型 | 模型体系 | 12 | 不等同随机 Cutout、监督 mask 或普通图像修复 |
| 候选分数 | candidate score | `A^k` | 高通亮点、当前稀疏状态与正背景残差经 RMS 归一化后的凸组合 | 无量纲 | 12.2 | 不称分割概率或置信度 |
| 软候选掩膜 | soft candidate mask | `C^k` | 由无标签分位阈值、温度和非平坦门生成的内部观测排除权 | `[0,1]` | 12.2 | 不与 ground-truth mask、最终预测混用 |
| 可靠背景观测权 | reliable-background observation weight | `V^k=1-C^k` | 当前阶段输入背景数据保真项的逐像素权重 | `[0,1]` | 12.2 | 不与数据集有效样本标记混用 |
| 非平坦门 | non-flat gate | `g_k` | 候选分数空间标准差与固定稳定尺度的比值，保证平坦输入不产生半幅掩膜 | `[0,1)` | 12.2 | 不称预测概率 |
| 候选面积日程 | candidate-area schedule | `varrho_k,L_budget` | 从 2% 到分辨率相关下界固定收缩的无标签候选面积日程及其软约束 | 比率 | 12.2 | 不是学习参数；不得用 test mask 调参 |
| 行随机环形背景先验 | row-stochastic annular background prior | `\mathcal A_k`, `B_A^{(k,j)}` | 排除候选观测后，以非负环形核和自回退项生成的常量保持背景侧信息及其内层缓存 | 与 `X` 同单位 | 12.3 | 不称地理 IDW 或自由 attention |
| 掩蔽补全目标 | masked-completion objective | `F_{(k,j)}(L)` | 联合可靠输入保真、当前分解锚定、候选区侧先验和 patch 低秩的条件背景目标 | 强度平方的加权和 | 12.4 | 不声称已精确最小化 |
| 内层补全状态 | inner completion state | `L^(k,j)` | 单个外层阶段中经梯度步与 patch-SVT denoising anchor 反复更新的背景状态 | 与 `X` 同单位 | 12.4 | `J=1` 只称 masked denoising |
| 正精度共识 | positive-precision consensus | `p_X,k,p_L,k,p_A,k` | 三个独立阶段标量融合可靠输入、补全状态和候选区侧先验 | 正常模型无量纲 `(0.25,4)` | 12.4 | `noside` 的 `p_A=0` 是预注册例外；不称概率或 attention |
| 反事实背景 | counterfactual background | `B_bar^(k+1)` | 候选像素不作为可靠输入观测时恢复的无目标背景估计 | 与 `X` 同单位 | 12.4 | 不称真实背景或因果真值 |
| 候选条件稀疏权重 | candidate-conditioned sparse weight | `W_CMC` | 在结构/稀疏 log 证据上由候选掩膜有界降低局部惩罚的正权重 | 无量纲 `[0.25,4]` | 12.5 | 不与 RCP 的可靠性权重混用 |
| 反事实一致性损失 | counterfactual consistency loss | `L_cf` | 候选区恢复背景与环形背景侧先验之间的归一化一致性惩罚 | 归一化强度 | 12.6 | 不称背景监督损失 |
